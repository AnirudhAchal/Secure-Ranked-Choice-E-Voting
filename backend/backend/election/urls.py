from django.urls import path
# from .views import ElectionList, ElectionDetail

app_name = 'election'

urlpatterns = [
    # path('<int:pk>/', ElectionDetail.as_view(), name='election_detail'),
    # path('', ElectionList.as_view(), name='election_list'),
]
